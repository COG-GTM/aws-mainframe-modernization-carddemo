# 05 - Decisions only the system owner can make

Static analysis of the repository surfaces the questions below. None of them can be answered from source alone, and none is answered here. Each row gives the evidence (`path:line`), what stays unresolved if the question is left open, and the role that plausibly owns the answer (application owner / data owner / hosting / security). The list is authored; the count in the next line is refreshed by `build_discovery.py`.

<!-- generated:decision-count -->
**17 decisions** are listed below; none is made here.
<!-- /generated:decision-count -->

| # | Decision | Evidence (`path:line`) | If left undecided | Plausible owner |
| --- | --- | --- | --- | --- |
| D1 | Is `CBTRN01C` dead, seasonal, or run from outside the repository? It is the only program with no JCL `EXEC`, no CSD definition, and no resolved call, yet it declares six files. | `app/cbl/CBTRN01C.cbl` (whole member); orphan table in `01-inventory.md` § "Programs with no caller"; its file declarations at `app/cbl/CBTRN01C.cbl:29-60` | The sustainment baseline cannot state whether the six files it declares have a live consumer; any later change to those record layouts has an unknown blast radius. | Application owner |
| D2 | Are the 9-integer-digit accumulators a business limit or a latent overflow? `WS-TEMP-BAL` (`S9(09)V99`) receives a sum of two `S9(10)V99` fields; `TRAN-CAT-BAL` and the report totals accumulate 9-digit amounts into 9-digit fields; none of the `ADD`/`COMPUTE` statements carries `ON SIZE ERROR`. | `app/cbl/CBTRN02C.cbl:187`, `app/cbl/CBTRN02C.cbl:403-405`, `app/cpy/CVACT01Y.cpy:13-14`; `app/cbl/CBTRN02C.cbl:508`, `app/cbl/CBTRN02C.cbl:527`, `app/cpy/CVTRA01Y.cpy:9`; `app/cbl/CBTRN03C.cbl:134-136`, `app/cbl/CBTRN03C.cbl:287-288`, `app/cbl/CBTRN03C.cbl:297` | Any balance or total crossing 999,999,999.99 is silently truncated; whether that ceiling is acceptable is not recorded anywhere. | Application owner, data owner |
| D3 | Is the missing `ROUNDED` on the interest computation a rounding rule (truncate to the cent) or a defect? | `app/cbl/CBACT04C.cbl:464-465`; receiving field `app/cbl/CBACT04C.cbl:168` | Interest posted by `CBACT04C` and the figure any independent recomputation would produce differ by up to one cent per category per run, and there is no documented rule to reconcile against. | Application owner |
| D4 | Which copy of the transaction history is the system of record: the KSDS `AWS.M2.CARDDEMO.TRANSACT.VSAM.KSDS`, the backup GDG `TRANSACT.BKUP`, the merged GDG `TRANSACT.COMBINED`, or the interest GDG `SYSTRAN`? `COMBTRAN` rebuilds the KSDS from `TRANSACT.BKUP(0)` merged with `SYSTRAN(0)`. | `app/jcl/COMBTRAN.jcl:22-26`, `app/jcl/COMBTRAN.jcl:37`, `app/jcl/COMBTRAN.jcl:41-46`; `app/jcl/TRANREPT.jcl:23-33`; `app/jcl/INTCALC.jcl:41`; `app/csd/CARDDEMO.CSD:76-77` | Retention, recovery point, and reconciliation obligations attach to whichever copy is authoritative; today online updates (`app/cbl/COTRN02C.cbl:713`) and batch reloads both write the KSDS. | Data owner, hosting |
| D5 | Are the three optional modules (`app-authorization-ims-db2-mq`, `app-transaction-type-db2`, `app-vsam-mq`) in the sustainment scope? They add 71 artifacts, the only `EXEC SQL`, the only DL/I and message-queue calls, and their own CSD groups. | module counts in `01-inventory.md` § "Artifacts by module"; `app/app-authorization-ims-db2-mq/cbl/COPAUA0C.cbl:262`, `app/app-authorization-ims-db2-mq/cbl/PAUDBLOD.CBL:244`, `app/app-transaction-type-db2/csd/CRDDEMOD.csd:11`, `app/app-vsam-mq/csd/CRDDEMOM.csd:1` | Every count in this dossier and every skills, licensing, and test-environment assumption that follows from it changes by the module totals. | Application owner |
| D6 | What do the batch date parameters mean and which one governs the report window? `CBACT04C` receives `PARM='2022071800'` and uses it only as a key prefix; `TRANREPT` hard-codes `C'2022-01-01'`/`C'2022-07-06'` in a SORT `INCLUDE`, then `CBTRN03C` reads a second window from `DATEPARM`, which has no sample content in the repository. | `app/jcl/INTCALC.jcl:22`, `app/cbl/CBACT04C.cbl:176-178`, `app/cbl/CBACT04C.cbl:476-480`; `app/jcl/TRANREPT.jcl:40-48`; `app/cbl/CBTRN03C.cbl:122-125`, `app/cbl/CBTRN03C.cbl:173-174`, `app/jcl/TRANREPT.jcl:73-74` | Operations cannot say which parameter to change to move the reporting period, and interest transaction IDs (`TRAN-ID` = `PARM-DATE` + counter) cannot be interpreted. | Application owner, hosting (scheduling) |
| D7 | Is the `NEXT SENTENCE` in the `CBTRN03C` window test intended? Read literally it transfers control past the `END-PERFORM.` period, so the first out-of-window record would end the report loop rather than be skipped. | `app/cbl/CBTRN03C.cbl:173-178`, `app/cbl/CBTRN03C.cbl:206` | If the input is already windowed by the SORT step (`app/jcl/TRANREPT.jcl:47-48`) the branch never fires; if `DATEPARM` is ever narrower than the SORT literals, reports end early with no error. Only a run against representative data settles which. | Application owner |
| D8 | Should the online amount limit match the batch limit? The screen accepts `-99999999.99` (8 integer digits) while every stored amount field allows 9. | `app/cbl/COTRN02C.cbl:340-343`, `app/cbl/COTRN02C.cbl:53`; `app/cpy/CVTRA05Y.cpy:10` | Batch-only amounts above 99,999,999.99 cannot be keyed or corrected online; whether that is policy or accident is undocumented. | Application owner |
| D9 | Is a date-only value acceptable in `TRAN-PROC-TS`/`TRAN-ORIG-TS`? Online adds store 10 bytes plus spaces; batch stores a 26-byte timestamp. Both satisfy the 10-byte comparisons in the SORT and in `CBTRN03C`. | `app/cbl/COTRN02C.cbl:464-465`, `app/cpy-bms/COTRN02.CPY:102`, `app/cpy-bms/COTRN02.CPY:108`; `app/cbl/CBTRN02C.cbl:438`; `app/jcl/TRANREPT.jcl:42`; `app/cbl/CBTRN03C.cbl:173-174` | Any consumer that parses the time portion sees blanks for online-entered records; ordering within a day is undefined for them. | Data owner |
| D10 | Which scheduler definition is authoritative? Two scheduler exports exist with different job sets: one covers `POSTTRAN`, the other covers `INTCALC → COMBTRAN`; `TRANREPT` appears in neither. | `app/scheduler/CardDemo.ca7:70-72`; `app/scheduler/CardDemo.controlm:65-83`; scheduler table in `01-inventory.md` § "Scheduler definitions" | The batch run order assumed in the lineage (`04-field-lineage.md` M14) stays Inferred; recovery and rerun procedures cannot be written from the repository. | Hosting (scheduling), application owner |
| D11 | Who owns the data-quality rule behind the expiry check? `CBTRN02C` compares `ACCT-EXPIRAION-DATE` (`X(10)`) with the first 10 bytes of the origin timestamp as text; neither operand is format-checked in that program. | `app/cbl/CBTRN02C.cbl:414`; `app/cpy/CVACT01Y.cpy:11`; `app/cpy/CVTRA06Y.cpy:16` | A malformed date in either field yields a silent accept or a reject with reason 103, and no one is designated to certify the upstream format. | Data owner |
| D12 | Which run-time targets are legitimate for the eight `XCTL` statements whose program name comes from a commarea variable? The generator cannot resolve them. | `app/cbl/COACTUPC.cbl:956`, `app/cbl/COACTVWC.cbl:349`, `app/cbl/COCRDLIC.cbl:538`, `app/cbl/COCRDLIC.cbl:566`, `app/cbl/COCRDSLC.cbl:331`, `app/cbl/COCRDUPC.cbl:473`, `app/app-transaction-type-db2/cbl/COTRTLIC.cbl:620`, `app/app-transaction-type-db2/cbl/COTRTUPC.cbl:457` | Eight `program->program` edges stay unresolved; the online navigation map is incomplete until a trace or an SME supplies the reachable set. | Application owner |
| D13 | Are the eleven CSD transactions that point at programs with no source (`COADM00C`, `COTSTP1C`-`COTSTP4C`, `COCRDSEC`) retired, held elsewhere, or missing from the repository? | `app/jcl/CBADMCDJ.jcl:130-156`; `app/csd/CARDDEMO.CSD:388`; table in `01-inventory.md` § "CSD transactions whose program has no source" | The CSD load job would define transactions that abend on first use, or the repository is incomplete; either way the inventory total is provisional. | Application owner |
| D14 | Is the sign-on credential store part of the sustainment baseline? `COSGN00C` reads the `USRSEC` file and compares the entered password with `SEC-USR-PWD PIC X(08)` directly. | `app/cbl/COSGN00C.cbl:135-136`, `app/cbl/COSGN00C.cbl:212`, `app/cbl/COSGN00C.cbl:223`; `app/cpy/CSUSR01Y.cpy:21` | Whether the credential handling is in scope for the security assessment, and who owns the `USRSEC` dataset's protection, remain unassigned. | Security, data owner |
| D15 | Which dataset names in the optional-module JCL are placeholders? One load library is named `XXXXXXXX.PROD.LOADLIB`; several others are outside the application's high-level qualifier. | `app/app-authorization-ims-db2-mq/jcl/CBPAUP0J.jcl:27`; library table in `02-dependency-map.md` § "Library and component datasets" | Those jobs cannot be run or even security-reviewed until the real names are supplied; the dependency map lists them as `unknown` mode. | Hosting |
| D16 | Are the eight datasets referenced only in JCL (alternate indexes, path entries, ESDS/RRDS variants of `USRSEC`, two `CLUSTER` names under a different qualifier) live objects or leftovers? | table in `01-inventory.md` § "Datasets referenced only in JCL"; `app/jcl/DEFCUST.jcl:25`, `app/jcl/DEFCUST.jcl:35`, `app/jcl/ESDSRRDS.jcl:72`, `app/jcl/ESDSRRDS.jcl:106` | Catalog clean-up and backup scope cannot be fixed; the orphan count in the README carries these as data-bearing datasets nobody opens. | Data owner, hosting |
| D17 | Should the copybook `UNUSED1Y` be retired? Nothing copies it. | `app/cpy/UNUSED1Y.cpy`; orphan table in `02-dependency-map.md` § "Copybooks nobody copies" | A 10-line member stays in the baseline with no owner; trivial in size, but it is the one copybook whose purpose the estate does not explain. | Application owner |
